/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/makeup-artist-portfolio',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
